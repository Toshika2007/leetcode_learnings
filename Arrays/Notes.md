## Two Sum

Learning:
- Learned how to use nested loops to check all pairs.
- Compared the sum of two elements with the target.
- Learned how to return an array in Java.

Time Complexity: O(n²)
Space Complexity: O(1)

## Single Number

Learning:
- Learned how to count the frequency of each element.
- Used nested loops to find the element that appears only once.
- Returned the element whose count is 1.

Time Complexity: O(n²)
Space Complexity: O(1)

## Contains Duplicate

Learning:
- Learned HashSet usage.
- Used contains() to find duplicates.
- HashSet stores unique values.

Time Complexity: O(n)
Space Complexity: O(n)